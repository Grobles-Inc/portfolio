---
import { SITE_TITLE } from "@/consts";
const currentLocale = Astro.currentLocale || "en";
---

<section class="max-w-7xl mx-auto">
  <div
    class="flex flex-col items-center text-center space-y-6 py-12 animate-fade-in"
  >
    <h1 class="text-4xl md:text-5xl lg:text-7xl font-bold">
      {currentLocale === "es" ? "Somos " : "We are "}
      <span class="italic text-primary">{SITE_TITLE}</span>
      {
        currentLocale === "es"
          ? ", startup líder en desarrollo de software"
          : ", startup leader in software development"
      }
    </h1>
    <p class="max-w-2xl text-lg">
      {
        currentLocale === "es"
          ? "Trabajamos contigo para convertir tus ideas en soluciones innovadoras, utilizando tecnología de punta para desarrollar productos de software escalable."
          : "We work with you to turn your ideas into innovative solutions, using cutting-edge technology to develop scalable software products."
      }
    </p>
    <span class="text-xl">
      {
        currentLocale === "es"
          ? "Construyamos el futuro juntos. 🚀"
          : "Let's build the future together. 🚀"
      }
    </span>
    <a
      href="/contact"
      class="btn btn-lg btn-primary font-semibold px-8 py-3 rounded-full transition-all duration-300 hover:scale-105"
    >
      {currentLocale === "es" ? "Comenzar ahora" : "Start now"}
    </a>
  </div>

  <div
    class="flex justify-center items-center px-4 sm:px-6 md:px-8 animate-slide-up"
  >
    <img
      src="https://images.unsplash.com/photo-1629904853716-f0bc54eea481?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
      alt={currentLocale === "es"
        ? "Equipo de desarrolladores de software trabajando en proyectos innovadores con tecnología moderna"
        : "Team of software developers working on innovative projects with modern technology"}
      width="900"
      height="900"
      class="rounded-[2rem] shadow-xl"
      loading="lazy"
      decoding="async"
    />
  </div>

  <div
    class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 py-12 px-4 text-lg animate-fade-in"
  >
    <div
      class="p-6 rounded-xl text-center bg-gradient-to-r from-primary to-secondary"
    >
      <h3 class="text-4xl font-bold mb-2">5+</h3>
      <p class="text-base-content/70 font-semibold">
        {currentLocale === "es" ? "Años de experiencia" : "Years of experience"}
      </p>
    </div>
    <div
      class="p-6 rounded-xl text-center bg-gradient-to-r from-primary to-secondary"
    >
      <h3 class="text-4xl font-bold mb-2">95%</h3>
      <p class="text-base-content/70 font-semibold">
        {
          currentLocale === "es"
            ? "Satisfacción del Cliente"
            : "Client satisfaction"
        }
      </p>
    </div>
    <div
      class="p-6 rounded-xl text-center bg-gradient-to-r from-primary to-secondary"
    >
      <h3 class="text-4xl font-bold mb-2">24x7</h3>
      <p class="text-base-content/70 font-semibold">
        {currentLocale === "es" ? "Soporte al cliente" : "Client support"}
      </p>
    </div>
  </div>
</section>
